fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
opentelemetry-api==1.34.1
opentelemetry-sdk==1.34.1

# Extensions (feat/gateway-extensions branch)
# BPE tokenization for token-ID prefix hashing (matches vLLM cache keys).
tokenizers>=0.20,<1.0
# Semantic cache and TTFT predictor math.
numpy>=1.26
# Optional (only needed when actually serving semantic cache / training models):
#   sentence-transformers>=3.0
#   joblib>=1.4
#   scikit-learn>=1.5
#   xgboost>=2.0
